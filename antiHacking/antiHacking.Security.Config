unit antiHacking.Security;

unit Security.Config;

interface

{$IFDEF DEBUG}
  {$DEFINE SECURITY_RELAXED}
{$ENDIF}

const
  // In protected release builds, set this at build-time (CI) after signing.
  // Keep it in a resource or external manifest in real life to avoid hardcoding edits.
  EXPECTED_EXE_SHA256_HEX = 'REPLACE_IN_CI';

implementation

end.


end.
